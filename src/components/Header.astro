---
const navItems = [
  { href: '/home', label: 'Home' },
  { href: '/services', label: 'Services' },
  { href: '/projects', label: 'Projects' },
  { href: '/about', label: 'About Me' },
  { href: '/', label: 'Contact Me' }
];
---

<header class="fixed inset-x-0 top-0 z-50 border-b border-cyan-400/20 bg-slate-950/85 backdrop-blur">
  <nav class="mx-auto flex w-full max-w-7xl items-center justify-between px-4 py-4 md:px-8">
    <a href="/" class="text-lg font-bold tracking-[0.2em] text-cyan-300 md:text-xl">HANDYMAN CRUZ</a>

    <ul class="hidden items-center gap-6 text-sm font-medium text-slate-100 md:flex">
      {navItems.map((item) => (
        <li>
          <a href={item.href} class="transition hover:text-cyan-300">{item.label}</a>
        </li>
      ))}
    </ul>
  </nav>
</header>

<style is:global>
  html {
    scroll-behavior: smooth;
  }
</style>
